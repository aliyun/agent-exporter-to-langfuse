{
  "name": "@agent-exporter-to-langfuse/cursor-hook",
  "version": "0.1.0",
  "private": true,
  "description": "Trace Cursor Agent sessions to Langfuse with langstash delivery",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "lint:tsc": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0",
    "@opentelemetry/sdk-trace-base": "^2.0.1",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^3.2.6",
    "tsdown": "^0.18.0",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
