[project]
name = "hooks"
version = "0.1.0"
description = "Claude Code Langfuse exporter hook"
requires-python = ">=3.11"
dependencies = [
    "langfuse>=4.7.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[dependency-groups]
dev = [
    "pytest-cov>=7.1.0",
]
