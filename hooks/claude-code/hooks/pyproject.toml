[project]
name = "hooks"
version = "0.1.0"
description = "Claude Code Langfuse exporter hook"
requires-python = ">=3.11"
dependencies = [
    "langfuse>=4.7.0",
    "langstash-deliver",
]

[tool.uv.sources]
langstash-deliver = { path = "../../langstash-deliver/python" }
